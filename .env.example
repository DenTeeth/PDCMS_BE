# ===========================================
# DENTAL CLINIC MANAGEMENT SYSTEM
# Environment Variables for DigitalOcean Deployment
# ===========================================

# ===========================================
# Application Configuration
# ===========================================
SPRING_PROFILES_ACTIVE=prod
APP_PORT=8080
FRONTEND_URL=https://your-frontend-domain.com

# ===========================================
# Database Configuration (PostgreSQL)
# ===========================================
DB_USERNAME=dentalclinic_user
DB_PASSWORD=your_secure_postgres_password_here
DB_DATABASE=dental_clinic_db
DB_PORT=5432

# ===========================================
# Redis Configuration
# ===========================================
REDIS_PASSWORD=your_secure_redis_password_here
REDIS_PORT=6379

# ===========================================
# JWT Configuration
# ===========================================
# Generate a strong secret: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_key_at_least_256_bits_here
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=2592000000

# ===========================================
# Email Configuration (Gmail SMTP)
# ===========================================
# Use App Password from Google Account Security
MAIL_USERNAME=dentalclinicPDCMS@gmail.com
MAIL_PASSWORD=your_gmail_app_password_here

# ===========================================
# DigitalOcean Specific
# ===========================================
# These will be auto-configured by DigitalOcean App Platform
# DATABASE_URL=postgresql://user:password@host:5432/database
# REDIS_URL=redis://:password@host:6379

# ===========================================
# Optional: Nginx Configuration
# ===========================================
# Uncomment if using nginx reverse proxy
# DOMAIN_NAME=api.your-domain.com
# SSL_EMAIL=admin@your-domain.com

# ===========================================
# NOTES FOR DEPLOYMENT
# ===========================================
# 1. Copy this file to .env and fill in real values
# 2. NEVER commit .env to git (already in .gitignore)
# 3. In DigitalOcean App Platform, set these as Environment Variables
# 4. Use strong passwords (minimum 32 characters)
# 5. JWT_SECRET should be at least 256 bits (32+ characters)
# 6. For Gmail: Enable 2FA and generate App Password
#    https://support.google.com/accounts/answer/185833
